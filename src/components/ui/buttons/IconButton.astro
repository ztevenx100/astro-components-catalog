---
import CodeViewer from '../../util/CodeViewer.astro';

const code = `
<button class="icon-button">
  <span class="icon">📧</span>
  <span class="text">Enviar Email</span>
</button>

<button class="icon-button icon-right">
  <span class="text">Siguiente</span>
  <span class="icon">→</span>
</button>

<button class="icon-button icon-only">
  <span class="icon">♥</span>
</button>

<style>
  .icon-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    padding: 6px 12px;
    border: 2px solid var(--primary-color);
    border-radius: 8px;
    background: linear-gradient(135deg, transparent 0%, rgba(var(--primary-color-rgb, 59, 130, 246), 0.05) 100%);
    color: var(--primary-color);
    font-size: 0.875rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    min-height: 40px;
    backdrop-filter: blur(10px);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06), 
                inset 0 1px 0 rgba(255, 255, 255, 0.1);
  }

  .icon-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
    transition: left 0.6s ease;
    z-index: 0;
  }

  .icon-button:hover::before {
    left: 100%;
  }

  .icon-button:hover {
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--accent-color, var(--primary-color)) 100%);
    color: white;
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 12px 35px rgba(var(--primary-color-rgb, 59, 130, 246), 0.4),
                0 4px 15px rgba(0, 0, 0, 0.1),
                inset 0 1px 0 rgba(255, 255, 255, 0.2);
    border-color: transparent;
  }

  .icon-button:active {
    transform: translateY(-1px) scale(1.01);
    box-shadow: 0 6px 20px rgba(var(--primary-color-rgb, 59, 130, 246), 0.3);
  }

  .icon-button .icon {
    font-size: 1.2rem;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 24px;
    background: rgba(var(--primary-color-rgb, 59, 130, 246), 0.1);
    border-radius: 8px;
    padding: 4px;
    position: relative;
    z-index: 1;
  }

  .icon-button .text {
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    z-index: 1;
    letter-spacing: 0.025em;
  }

  .icon-button:hover .icon {
    transform: scale(1.15) rotate(5deg);
    background: rgba(255, 255, 255, 0.2);
    box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.3);
  }

  .icon-button:hover .text {
    transform: translateX(2px);
    text-shadow: 0 0 8px rgba(255, 255, 255, 0.5);
  }

  .icon-button.icon-right {
    flex-direction: row;
  }

  .icon-button.icon-right:hover .icon {
    transform: translateX(4px) scale(1.15);
    rotate: -5deg;
  }

  .icon-button.icon-only {
    padding: 14px;
    border-radius: 50%;
    min-width: 52px;
    min-height: 52px;
    gap: 0;
    background: linear-gradient(135deg, var(--surface-color) 0%, var(--surface-secondary) 100%);
  }

  .icon-button.icon-only .icon {
    font-size: 1.3rem;
    background: linear-gradient(135deg, rgba(var(--primary-color-rgb, 59, 130, 246), 0.15), rgba(var(--primary-color-rgb, 59, 130, 246), 0.05));
    border-radius: 50%;
    padding: 8px;
  }

  .icon-button.icon-only:hover .icon {
    transform: scale(1.25) rotate(12deg);
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.25), rgba(255, 255, 255, 0.1));
    box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.4);
  }

  .icon-button.success {
    border-color: #10b981;
    color: #10b981;
    background: linear-gradient(135deg, transparent 0%, rgba(16, 185, 129, 0.05) 100%);
  }

  .icon-button.success:hover {
    background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    color: white;
    border-color: transparent;
    box-shadow: 0 12px 35px rgba(16, 185, 129, 0.4);
  }

  .icon-button.danger {
    border-color: #ef4444;
    color: #ef4444;
    background: linear-gradient(135deg, transparent 0%, rgba(239, 68, 68, 0.05) 100%);
  }

  .icon-button.danger:hover {
    background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
    color: white;
    border-color: transparent;
    box-shadow: 0 12px 35px rgba(239, 68, 68, 0.4);
  }

  .icon-button.warning {
    border-color: #f59e0b;
    color: #f59e0b;
    background: linear-gradient(135deg, transparent 0%, rgba(245, 158, 11, 0.05) 100%);
  }

  .icon-button.warning:hover {
    background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
    color: white;
    border-color: transparent;
    box-shadow: 0 12px 35px rgba(245, 158, 11, 0.4);
  }

  /* Responsive mejorado */
  @media (max-width: 768px) {
    .icon-button {
      padding: 12px 24px;
      font-size: 0.9rem;
      min-height: 48px;
      gap: 0.5rem;
    }

    .icon-button.icon-only {
      min-width: 48px;
      min-height: 48px;
      padding: 12px;
    }

    .icon-button:hover {
      transform: translateY(-2px) scale(1.01);
    }
  }
</style>
`;
---

<div class="component-preview">
  <h3>Botones con Iconos</h3>
  <p>Botones versátiles con iconos, múltiples variantes y estados interactivos.</p>
  
  <div class="demo-container">
    <!-- Sección principal con grid responsivo -->
    <div class="main-demo-grid">
      <div class="demo-section">
        <div class="section-header">
          <h4>Botones con Texto</h4>
          <p class="section-description">Botones con iconos y texto descriptivo</p>
        </div>
        <div class="button-showcase">
          <button class="icon-button">
            <span class="icon">📧</span>
            <span class="text">Enviar Email</span>
          </button>
          
          <button class="icon-button icon-right">
            <span class="text">Siguiente</span>
            <span class="icon">→</span>
          </button>
          
          <button class="icon-button">
            <span class="icon">💾</span>
            <span class="text">Guardar</span>
          </button>
        </div>
      </div>

      <div class="demo-section">
        <div class="section-header">
          <h4>Solo Iconos</h4>
          <p class="section-description">Botones compactos con iconos únicamente</p>
        </div>
        <div class="button-showcase icon-grid">
          <button class="icon-button icon-only" title="Favorito">
            <span class="icon">♥</span>
          </button>
          
          <button class="icon-button icon-only success" title="Confirmar">
            <span class="icon">✓</span>
          </button>
          
          <button class="icon-button icon-only danger" title="Eliminar">
            <span class="icon">×</span>
          </button>
          
          <button class="icon-button icon-only" title="Configuración">
            <span class="icon">⚙️</span>
          </button>
          
          <button class="icon-button icon-only" title="Buscar">
            <span class="icon">🔍</span>
          </button>
          
          <button class="icon-button icon-only" title="Compartir">
            <span class="icon">📤</span>
          </button>
        </div>
      </div>
    </div>

    <!-- Sección de variantes con mejor espaciado -->
    <div class="variants-section">
      <div class="section-header centered">
        <h4>Variantes de Estilo</h4>
        <p class="section-description">Diferentes estilos y efectos visuales</p>
      </div>
      
      <div class="variants-grid">
        <div class="variant-group">
          <span class="variant-label">Relleno</span>
          <button class="icon-button filled">
            <span class="icon">⚡</span>
            <span class="text">Acción Principal</span>
          </button>
        </div>
        
        <div class="variant-group">
          <span class="variant-label">Suave</span>
          <button class="icon-button soft">
            <span class="icon">🌟</span>
            <span class="text">Acción Suave</span>
          </button>
        </div>
        
        <div class="variant-group">
          <span class="variant-label">Fantasma</span>
          <button class="icon-button ghost">
            <span class="icon">👻</span>
            <span class="text">Acción Sutil</span>
          </button>
        </div>
      </div>
    </div>

    <!-- Sección de estados especiales -->
    <div class="states-section">
      <div class="section-header centered">
        <h4>Estados Especiales</h4>
        <p class="section-description">Botones con diferentes estados y colores</p>
      </div>
      
      <div class="states-grid">
        <button class="icon-button success">
          <span class="icon">✅</span>
          <span class="text">Éxito</span>
        </button>
        
        <button class="icon-button danger">
          <span class="icon">⚠️</span>
          <span class="text">Peligro</span>
        </button>
        
        <button class="icon-button warning">
          <span class="icon">⚡</span>
          <span class="text">Advertencia</span>
        </button>
        
        <button class="icon-button" disabled>
          <span class="icon">🔒</span>
          <span class="text">Deshabilitado</span>
        </button>
      </div>
    </div>
  </div>
</div>

<CodeViewer code={code} />

<style>
  /* Estilos duplicados para el funcionamiento del preview */
  .icon-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.625rem;
    padding: 6px 12px;
    border: 2px solid var(--primary-color);
    border-radius: 8px;
    background: linear-gradient(135deg, transparent 0%, rgba(var(--primary-color-rgb, 59, 130, 246), 0.05) 100%);
    color: var(--primary-color);
    font-size: 0.875rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    min-height: 40px;
    min-width: fit-content;
    backdrop-filter: blur(10px);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06), 
                inset 0 1px 0 rgba(255, 255, 255, 0.1);
  }

  .icon-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
    transition: left 0.6s ease;
    z-index: 0;
  }

  .icon-button:hover::before {
    left: 100%;
  }

  .icon-button:hover {
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--accent-color, var(--primary-color)) 100%);
    color: white;
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 12px 35px rgba(var(--primary-color-rgb, 59, 130, 246), 0.4),
                0 4px 15px rgba(0, 0, 0, 0.1),
                inset 0 1px 0 rgba(255, 255, 255, 0.2);
    border-color: transparent;
  }

  .icon-button:active {
    transform: translateY(-1px) scale(1.01);
    box-shadow: 0 6px 20px rgba(var(--primary-color-rgb, 59, 130, 246), 0.3);
  }

  .icon-button .icon {
    font-size: 1.2rem;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 24px;
    background: rgba(var(--primary-color-rgb, 59, 130, 246), 0.1);
    border-radius: 8px;
    padding: 4px;
    position: relative;
    z-index: 1;
  }

  .icon-button .text {
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    z-index: 1;
    letter-spacing: 0.025em;
  }

  .icon-button:hover .icon {
    transform: scale(1.15) rotate(5deg);
    background: rgba(255, 255, 255, 0.2);
    box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.3);
  }

  .icon-button:hover .text {
    transform: translateX(2px);
    text-shadow: 0 0 8px rgba(255, 255, 255, 0.5);
  }

  .icon-button.icon-right {
    flex-direction: row;
  }

  .icon-button.icon-right:hover .icon {
    transform: translateX(4px) scale(1.15);
    rotate: -5deg;
  }

  .icon-button.icon-only {
    padding: 14px;
    border-radius: 50%;
    min-width: 52px;
    min-height: 52px;
    gap: 0;
    background: linear-gradient(135deg, var(--surface-color) 0%, var(--surface-secondary) 100%);
  }

  .icon-button.icon-only .icon {
    font-size: 1.3rem;
    background: linear-gradient(135deg, rgba(var(--primary-color-rgb, 59, 130, 246), 0.15), rgba(var(--primary-color-rgb, 59, 130, 246), 0.05));
    border-radius: 50%;
    padding: 8px;
  }

  .icon-button.icon-only:hover .icon {
    transform: scale(1.25) rotate(12deg);
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.25), rgba(255, 255, 255, 0.1));
    box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.4);
  }

  /* Variantes mejoradas - estilos duplicados */
  .icon-button.filled {
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--accent-color, var(--primary-color)) 100%);
    color: white;
    border-color: transparent;
    box-shadow: 0 4px 15px rgba(var(--primary-color-rgb, 59, 130, 246), 0.3),
                inset 0 1px 0 rgba(255, 255, 255, 0.2);
  }

  .icon-button.filled .icon {
    background: rgba(255, 255, 255, 0.2);
  }

  .icon-button.filled:hover {
    background: linear-gradient(135deg, var(--accent-color, var(--primary-color)) 0%, var(--primary-color) 100%);
    transform: translateY(-4px) scale(1.03);
    box-shadow: 0 15px 40px rgba(var(--primary-color-rgb, 59, 130, 246), 0.4),
                0 8px 25px rgba(0, 0, 0, 0.15),
                inset 0 1px 0 rgba(255, 255, 255, 0.3);
    filter: brightness(1.1) saturate(1.1);
  }

  .icon-button.ghost {
    border: 2px solid transparent;
    background: linear-gradient(135deg, var(--surface-secondary) 0%, var(--surface-color) 100%);
    color: var(--text-color);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05),
                inset 0 1px 0 rgba(255, 255, 255, 0.1);
  }

  .icon-button.ghost .icon {
    background: var(--surface-secondary);
  }

  .icon-button.ghost:hover {
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--accent-color, var(--primary-color)) 100%);
    color: white;
    box-shadow: 0 12px 35px rgba(var(--primary-color-rgb, 59, 130, 246), 0.3);
  }

  .icon-button.soft {
    background: linear-gradient(135deg, 
                rgba(var(--primary-color-rgb, 59, 130, 246), 0.15) 0%, 
                rgba(var(--primary-color-rgb, 59, 130, 246), 0.08) 100%);
    border-color: rgba(var(--primary-color-rgb, 59, 130, 246), 0.2);
    color: var(--primary-color);
    backdrop-filter: blur(15px);
  }

  .icon-button.soft:hover {
    background: linear-gradient(135deg, 
                rgba(var(--primary-color-rgb, 59, 130, 246), 0.25) 0%, 
                rgba(var(--primary-color-rgb, 59, 130, 246), 0.15) 100%);
    border-color: rgba(var(--primary-color-rgb, 59, 130, 246), 0.4);
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 8px 25px rgba(var(--primary-color-rgb, 59, 130, 246), 0.25);
  }

  .icon-button.success {
    border-color: #10b981;
    color: #10b981;
    
    &:hover {
      background: #10b981;
      color: white;
      box-shadow: 0 6px 20px rgba(16, 185, 129, 0.3);
    }
  }

  .icon-button.danger {
    border-color: #ef4444;
    color: #ef4444;
    
    &:hover {
      background: #ef4444;
      color: white;
      box-shadow: 0 6px 20px rgba(239, 68, 68, 0.3);
    }
  }

  .icon-button.warning {
    border-color: #f59e0b;
    color: #f59e0b;
  }

  .icon-button.warning:hover {
    background: #f59e0b;
    color: white;
    box-shadow: 0 6px 20px rgba(245, 158, 11, 0.3);
  }

  .icon-button:focus {
    outline: none;
    box-shadow: 0 0 0 3px rgba(var(--primary-color-rgb, 59, 130, 246), 0.3);
  }

  .icon-button:disabled {
    opacity: 0.5;
    cursor: not-allowed;
    transform: none;
  }

  .icon-button:disabled:hover {
    background: transparent;
    color: var(--primary-color);
    transform: none;
    box-shadow: none;
  }

  .icon-button.loading .icon {
    animation: spin 1s linear infinite;
  }

  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  @media (max-width: 768px) {
    .icon-button {
      padding: 10px 20px;
      font-size: 0.9rem;
      min-height: 40px;
      
      &.icon-only {
        min-width: 40px;
        min-height: 40px;
        padding: 10px;
      }
    }
  }

  /* Estilos del contenedor preview mejorados */
  .component-preview {
    padding: 2rem 1rem;
    border: 1px solid var(--border-color);
    border-radius: 16px;
    background: linear-gradient(135deg, var(--background-color) 0%, var(--surface-color) 100%);
    transition: all 0.3s ease;
    position: relative;
    overflow: visible;
    
    h3 {
      margin: 0 0 0.5rem 0;
      color: var(--text-color);
      font-size: 1.5rem;
      font-weight: 700;
      text-align: center;
      background: linear-gradient(135deg, var(--text-color), var(--primary-color));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    p {
      margin: 0 0 1.5rem 0;
      color: var(--text-secondary);
      line-height: 1.6;
      text-align: center;
      font-size: 1rem;
    }
  }

  .demo-container {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  /* Grid principal para secciones */
  .main-demo-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 1.5rem;
    margin-bottom: 1.5rem;
    align-items: start;
  }

  .demo-section {
    max-width: 530px;
    background: var(--surface-color);
    border-radius: 16px;
    padding: 1.5rem;
    border: 1px solid var(--border-color);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
    transition: all 0.3s ease;
    min-height: auto;
    height: auto;
    display: flex;
    flex-direction: column;
    overflow: visible;
  
    &:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
    }
  }

  .section-header {
    margin-bottom: 1rem;
    text-align: left;
    flex-shrink: 0;
  }

  .section-header.centered {
    text-align: center;
  }

  .section-header h4 {
    margin: 0 0 0.5rem 0;
    color: var(--text-color);
    font-size: 1.1rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .section-header h4::before {
    content: '';
    width: 4px;
    height: 18px;
    background: linear-gradient(135deg, var(--primary-color), var(--accent-color, var(--primary-color)));
    border-radius: 2px;
  }

  .section-header.centered h4 {
    justify-content: center;
  }

  .section-header.centered h4::before {
    display: none;
  }

  .section-description {
    margin: 0;
    color: var(--text-secondary);
    font-size: 0.85rem;
    line-height: 1.4;
  }

  /* Showcase de botones */
  .button-showcase {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    justify-content: flex-start;
    align-items: flex-start;
    flex: 1;
    min-height: 0;
    width: 100%;
    
    &.icon-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(60px, 1fr));
      gap: 0.75rem;
      justify-items: center;
      align-content: start;
      grid-auto-rows: min-content;
    }
  }

  /* Secciones especializadas */
  .variants-section,
  .states-section {
    max-width: 530px;
    background: var(--surface-color);
    border-radius: 20px;
    padding: 1rem 0.5rem;
    border: 1px solid var(--border-color);
    box-shadow: 0 6px 25px rgba(0, 0, 0, 0.08);
  }

  .variants-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1rem;
    justify-items: center;
  }

  .variant-group {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem;
    background: var(--background-color);
    border-radius: 12px;
    border: 1px solid var(--border-color);
    transition: all 0.3s ease;
    min-width: 200px;
    width: 100%;
    max-width: 240px;
    
    &:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(var(--primary-color-rgb, 59, 130, 246), 0.1);
    }
  }

  .variant-label {
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 0.5rem;
  }

  .states-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 1.25rem;
    justify-items: center;
    max-width: 720px;
    margin: 0 auto;
  }

  /* Responsive mejorado */
  @media (max-width: 1024px) {
    .main-demo-grid {
      grid-template-columns: 1fr;
      gap: 1.25rem;
    }

    .variants-grid {
      grid-template-columns: 1fr;
      gap: 0.5rem;
    }

    .demo-section {
      padding: 1.25rem;
    }

    .variants-section,
    .states-section {
      padding: 1.25rem;
    }
  }

  @media (max-width: 768px) {
    .component-preview {
      padding: 1.5rem 1rem;
    }

    .demo-container {
      gap: 1.5rem;
    }

    .main-demo-grid {
      gap: 1rem;
      grid-template-columns: 1fr;
    }

    .demo-section {
      padding: 1.5rem;
      min-height: auto;
      height: auto;
    }

    .section-header {
      margin-bottom: 1rem;
    }

    .section-header h4 {
      font-size: 1rem;
      gap: 0.375rem;
    }

    .section-header h4::before {
      width: 3px;
      height: 16px;
    }

    .section-description {
      font-size: 0.8rem;
    }

    .button-showcase {
      gap: 0.75rem;
      justify-content: center;
      padding-bottom: 0.75rem;
    }

    .button-showcase.icon-grid {
      grid-template-columns: repeat(auto-fill, minmax(44px, 1fr));
      gap: 0.75rem;
      max-width: none;
      margin: 0;
    }

    .variants-section,
    .states-section {
      padding: 1.25rem;
    }

    .variant-group {
      min-width: auto;
      width: 100%;
      padding: 1rem;
      max-width: none;
    }

    .states-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
      max-width: 360px;
    }

    .icon-button {
      padding: 6px 12px;
      font-size: 0.8rem;
      min-height: 40px;
      gap: 0.5rem;
    }

    .icon-button .icon {
      font-size: 1rem;
    }

    .icon-button.icon-only {
      min-width: 40px;
      min-height: 40px;
      padding: 8px;
    }

    .icon-button.icon-only .icon {
      font-size: 1rem;
      
      &:hover {
        transform: translateY(-1px) scale(1.01);
      }

      &:active {
        transform: translateY(0);
      }
    }
  }

  @media (max-width: 480px) {
    .component-preview {
      padding: 1.25rem 0.75rem;
    }

    .component-preview h3 {
      font-size: 1.2rem;
    }

    .demo-container {
      gap: 1.25rem;
    }

    .demo-section {
      padding: 1.25rem;
      min-height: auto;
    }

    .states-grid {
      grid-template-columns: 1fr;
      max-width: 250px;
    }

    .button-showcase.icon-grid {
      grid-template-columns: repeat(3, 1fr);
      max-width: none;
    }

    .variant-group {
      padding: 0.875rem;
    }
  }

  /* Mejoras de accesibilidad y estados adicionales */
  .icon-button:focus-visible {
    outline: none;
    box-shadow: 0 0 0 3px rgba(var(--primary-color-rgb, 59, 130, 246), 0.4),
                0 0 0 6px rgba(var(--primary-color-rgb, 59, 130, 246), 0.1);
    transform: translateY(-1px);
  }

  .icon-button:disabled {
    opacity: 0.5;
    cursor: not-allowed;
    transform: none;
    filter: grayscale(0.5);
  }

  .icon-button:disabled:hover {
    background: linear-gradient(135deg, transparent 0%, rgba(var(--primary-color-rgb, 59, 130, 246), 0.05) 100%);
    color: var(--primary-color);
    transform: none;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
  }

  .icon-button:disabled .icon {
    transform: none;
    background: rgba(var(--primary-color-rgb, 59, 130, 246), 0.1);
  }

  /* Animación de entrada mejorada */
  .icon-button {
    animation: buttonFadeIn 0.5s ease-out;
  }

  @keyframes buttonFadeIn {
    from {
      opacity: 0;
      transform: translateY(10px) scale(0.95);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  /* Tema oscuro mejorado */
  html.dark .icon-button {
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2),
                inset 0 1px 0 rgba(255, 255, 255, 0.05);
    
    &:hover {
      box-shadow: 0 12px 35px rgba(var(--primary-color-rgb, 59, 130, 246), 0.3),
                  0 4px 15px rgba(0, 0, 0, 0.3),
                  inset 0 1px 0 rgba(255, 255, 255, 0.15);
    }
    
    &.ghost {
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3),
                  inset 0 1px 0 rgba(255, 255, 255, 0.05);
    }
  }
</style>
