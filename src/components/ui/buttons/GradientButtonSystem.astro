---
import CodeViewer from '../../util/CodeViewer.astro';

const code = `
<!-- Botones con Gradiente usando el Sistema Unificado -->
<button class="btn btn-gradient-primary">Gradiente Primario</button>
<button class="btn btn-gradient-success">Gradiente Éxito</button>
<button class="btn btn-gradient-danger">Gradiente Peligro</button>
<button class="btn btn-gradient-primary btn-lg">Gradiente Grande</button>
<button class="btn btn-gradient-primary btn-animated">Con Animación</button>

<!-- Con efectos especiales -->
<button class="btn btn-gradient-rainbow">Arcoíris</button>
<button class="btn btn-gradient-sunset">Atardecer</button>
<button class="btn btn-gradient-ocean">Océano</button>

<style>
  /* Extensiones del sistema base para gradientes */
  .btn-gradient-primary {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border-color: transparent;
    color: white;
    position: relative;
    overflow: hidden;
  }

  .btn-gradient-primary::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
    transition: left 0.6s ease;
  }

  .btn-gradient-primary:hover::before {
    left: 100%;
  }

  .btn-gradient-success {
    background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    border-color: transparent;
    color: white;
  }

  .btn-gradient-danger {
    background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
    border-color: transparent;
    color: white;
  }

  .btn-gradient-rainbow {
    background: linear-gradient(135deg, #ff6b6b, #4ecdc4, #45b7d1, #96ceb4, #feca57);
    border-color: transparent;
    color: white;
    animation: rainbow-shift 3s ease-in-out infinite;
  }

  .btn-gradient-sunset {
    background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 50%, #fecfef 100%);
    border-color: transparent;
    color: #333;
  }

  .btn-gradient-ocean {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border-color: transparent;
    color: white;
  }

  .btn-animated {
    animation: pulse-glow 2s ease-in-out infinite alternate;
  }

  @keyframes rainbow-shift {
    0% { filter: hue-rotate(0deg); }
    50% { filter: hue-rotate(180deg); }
    100% { filter: hue-rotate(360deg); }
  }

  @keyframes pulse-glow {
    0% { box-shadow: 0 0 20px rgba(102, 126, 234, 0.4); }
    100% { box-shadow: 0 0 30px rgba(102, 126, 234, 0.8); }
  }
</style>
`;
---

<div class="component-preview">
  <h3>🌈 Botones con Gradiente Mejorados</h3>
  <p>Extensión del sistema base con efectos de gradiente, shimmer y animaciones especiales.</p>
  
  <div class="demo-container">
    <div class="gradient-group">
      <h4>🎨 Gradientes Básicos</h4>
      <div class="gradient-showcase">
        <button class="btn btn-gradient-primary">
          <span class="btn-text">Gradiente Primario</span>
        </button>
        <button class="btn btn-gradient-success">
          <span class="btn-text">Gradiente Éxito</span>
        </button>
        <button class="btn btn-gradient-danger">
          <span class="btn-text">Gradiente Peligro</span>
        </button>
      </div>
    </div>

    <div class="gradient-group">
      <h4>📏 Tamaños con Gradiente</h4>
      <div class="gradient-showcase">
        <button class="btn btn-gradient-primary btn-sm">Pequeño</button>
        <button class="btn btn-gradient-primary btn-md">Mediano</button>
        <button class="btn btn-gradient-primary btn-lg">Grande</button>
      </div>
    </div>

    <div class="gradient-group">
      <h4>✨ Efectos Especiales</h4>
      <div class="gradient-showcase">
        <button class="btn btn-gradient-rainbow">Arcoíris</button>
        <button class="btn btn-gradient-sunset">Atardecer</button>
        <button class="btn btn-gradient-ocean">Océano</button>
      </div>
    </div>

    <div class="gradient-group">
      <h4>🎭 Con Animaciones</h4>
      <div class="gradient-showcase">
        <button class="btn btn-gradient-primary btn-animated">Pulso Brillante</button>
        <button class="btn btn-gradient-primary" disabled>Deshabilitado</button>
      </div>
    </div>
  </div>
</div>

<CodeViewer code={code} />

<style>
  /* Extensiones del sistema base - mantiene compatibilidad total */
  .btn-gradient-primary {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border-color: transparent;
    color: white;
    position: relative;
    overflow: hidden;
  }

  .btn-gradient-primary::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
    transition: left 0.6s ease;
    z-index: 1;
  }

  .btn-gradient-primary:hover::before {
    left: 100%;
  }

  .btn-text {
    position: relative;
    z-index: 2;
  }

  .btn-gradient-success {
    background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    border-color: transparent;
    color: white;
    position: relative;
    overflow: hidden;
  }

  .btn-gradient-danger {
    background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
    border-color: transparent;
    color: white;
    position: relative;
    overflow: hidden;
  }

  .btn-gradient-rainbow {
    background: linear-gradient(135deg, #ff6b6b, #4ecdc4, #45b7d1, #96ceb4, #feca57);
    border-color: transparent;
    color: white;
    animation: rainbow-shift 3s ease-in-out infinite;
    position: relative;
    overflow: hidden;
  }

  .btn-gradient-sunset {
    background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 50%, #fecfef 100%);
    border-color: transparent;
    color: #333;
    position: relative;
    overflow: hidden;
  }

  .btn-gradient-ocean {
    background: linear-gradient(135deg, #2196F3 0%, #21CBF3 100%);
    border-color: transparent;
    color: white;
    position: relative;
    overflow: hidden;
  }

  .btn-animated {
    animation: pulse-glow 2s ease-in-out infinite alternate;
  }

  @keyframes rainbow-shift {
    0% { filter: hue-rotate(0deg); }
    50% { filter: hue-rotate(180deg); }
    100% { filter: hue-rotate(360deg); }
  }

  @keyframes pulse-glow {
    0% { box-shadow: 0 0 20px rgba(102, 126, 234, 0.4); }
    100% { box-shadow: 0 0 30px rgba(102, 126, 234, 0.8); }
  }

  /* Estilos del contenedor preview */
  .component-preview {
    padding: 2rem;
    border: 1px solid var(--border-color);
    border-radius: 16px;
    background: linear-gradient(135deg, var(--background-color) 0%, var(--surface-color) 100%);
    margin-bottom: 2rem;
  }

  .component-preview h3 {
    margin: 0 0 0.5rem 0;
    color: var(--text-color);
    font-size: 1.5rem;
    font-weight: 700;
    text-align: center;
  }

  .component-preview p {
    margin: 0 0 1.5rem 0;
    color: var(--text-secondary);
    text-align: center;
    font-size: 1rem;
  }

  .demo-container {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .gradient-group {
    background: var(--surface-color);
    border-radius: 12px;
    padding: 1.5rem;
    border: 1px solid var(--border-color);
  }

  .gradient-group h4 {
    margin: 0 0 1rem 0;
    color: var(--text-color);
    font-size: 1.1rem;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .gradient-showcase {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
  }

  @media (max-width: 768px) {
    .component-preview {
      padding: 1.5rem;
    }

    .gradient-group {
      padding: 1rem;
    }

    .gradient-showcase {
      gap: 0.5rem;
    }
  }
</style>
