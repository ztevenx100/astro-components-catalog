---
import CodeViewer from '../../util/CodeViewer.astro';
import '../../../styles/card-base.css';

const code = `
<!-- Modal Básico -->
<button onclick="openModal('basicModal')" class="modal-trigger">Abrir Modal Básico</button>

<div id="basicModal" class="modal">
  <div class="modal-backdrop" onclick="closeModal('basicModal')"></div>
  <div class="modal-content">
    <div class="modal-header">
      <h3 class="modal-title">Título del Modal</h3>
      <button class="modal-close" onclick="closeModal('basicModal')">×</button>
    </div>
    <div class="modal-body">
      <p>Este es el contenido del modal. Aquí puedes agregar cualquier tipo de contenido.</p>
    </div>
    <div class="modal-footer">
      <button class="btn-secondary" onclick="closeModal('basicModal')">Cancelar</button>
      <button class="btn-primary" onclick="closeModal('basicModal')">Aceptar</button>
    </div>
  </div>
</div>

<!-- Modal Grande -->
<button onclick="openModal('largeModal')" class="modal-trigger">Modal Grande</button>

<div id="largeModal" class="modal large">
  <div class="modal-backdrop" onclick="closeModal('largeModal')"></div>
  <div class="modal-content">
    <div class="modal-header">
      <h3 class="modal-title">Modal Grande</h3>
      <button class="modal-close" onclick="closeModal('largeModal')">×</button>
    </div>
    <div class="modal-body">
      <h4>Contenido Extenso</h4>
      <p>Este modal tiene más espacio para contenido extenso. Perfecto para formularios largos o información detallada.</p>
      <div class="form-group">
        <label>Nombre completo:</label>
        <input type="text" placeholder="Ingresa tu nombre">
      </div>
      <div class="form-group">
        <label>Email:</label>
        <input type="email" placeholder="tu@email.com">
      </div>
      <div class="form-group">
        <label>Mensaje:</label>
        <textarea rows="4" placeholder="Escribe tu mensaje aquí..."></textarea>
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn-secondary" onclick="closeModal('largeModal')">Cancelar</button>
      <button class="btn-primary">Enviar</button>
    </div>
  </div>
</div>

<style>
  .modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .modal.show {
    opacity: 1;
    visibility: visible;
  }

  .modal-backdrop {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(4px);
  }

  .modal-content {
    background: var(--surface-color);
    border-radius: 16px;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    max-width: 500px;
    width: 90%;
    max-height: 90vh;
    overflow: hidden;
    transform: scale(0.9);
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    z-index: 1001;
  }

  .modal.show .modal-content {
    transform: scale(1);
  }

  .modal.large .modal-content {
    max-width: 800px;
  }

  .modal-header {
    padding: 1.5rem;
    border-bottom: 1px solid var(--border-color);
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .modal-title {
    margin: 0;
    color: var(--text-color);
    font-size: 1.25rem;
    font-weight: 700;
  }

  .modal-close {
    width: 32px;
    height: 32px;
    border: none;
    background: transparent;
    color: var(--text-secondary);
    font-size: 1.5rem;
    cursor: pointer;
    border-radius: 6px;
    transition: all 0.3s ease;
  }

  .modal-close:hover {
    background: var(--surface-secondary);
    color: var(--text-color);
  }

  .modal-body {
    padding: 1.5rem;
    overflow-y: auto;
    max-height: 60vh;
  }

  .modal-footer {
    padding: 1.5rem;
    border-top: 1px solid var(--border-color);
    display: flex;
    gap: 0.75rem;
    justify-content: flex-end;
  }
</style>

<script>
function openModal(modalId) {
  const modal = document.getElementById(modalId);
  if (modal) {
    modal.classList.add('show');
    document.body.style.overflow = 'hidden';
  }
}

function closeModal(modalId) {
  const modal = document.getElementById(modalId);
  if (modal) {
    modal.classList.remove('show');
    document.body.style.overflow = 'auto';
  }
}

// Cerrar modal con ESC
document.addEventListener('keydown', function(e) {
  if (e.key === 'Escape') {
    const openModal = document.querySelector('.modal.show');
    if (openModal) {
      openModal.classList.remove('show');
      document.body.style.overflow = 'auto';
    }
  }
});
</script>
`;
---

<div class="component-preview">
  <h3>Modal/Dialog Components</h3>
  <p>Ventanas modales con diferentes tamaños y funcionalidades, con animaciones suaves y accesibilidad.</p>
  
  <div class="demo-container">
    <div class="modal-showcase">
      
      <div class="modal-section">
        <h4>Tamaños de Modal</h4>
        <div class="modal-controls">
          <button onclick="openModal('basicModal')" class="modal-trigger">Modal Básico</button>
          <button onclick="openModal('largeModal')" class="modal-trigger">Modal Grande</button>
          <button onclick="openModal('confirmModal')" class="modal-trigger">Modal de Confirmación</button>
          <button onclick="openModal('fullscreenModal')" class="modal-trigger">Modal Pantalla Completa</button>
        </div>
      </div>

    </div>
  </div>
</div>

<!-- Modales -->
<div id="basicModal" class="modal">
  <div class="modal-backdrop" onclick="closeModal('basicModal')"></div>
  <div class="modal-content">
    <div class="modal-header">
      <h3 class="modal-title">Modal Básico</h3>
      <button class="modal-close" onclick="closeModal('basicModal')">×</button>
    </div>
    <div class="modal-body">
      <p>Este es un modal básico con contenido simple. Perfecto para mostrar información rápida o confirmaciones sencillas.</p>
    </div>
    <div class="modal-footer">
      <button class="btn-secondary" onclick="closeModal('basicModal')">Cancelar</button>
      <button class="btn-primary" onclick="closeModal('basicModal')">Aceptar</button>
    </div>
  </div>
</div>

<div id="largeModal" class="modal large">
  <div class="modal-backdrop" onclick="closeModal('largeModal')"></div>
  <div class="modal-content">
    <div class="modal-header">
      <h3 class="modal-title">Modal Grande con Formulario</h3>
      <button class="modal-close" onclick="closeModal('largeModal')">×</button>
    </div>
    <div class="modal-body">
      <div class="form-group">
        <label>Nombre completo:</label>
        <input type="text" placeholder="Ingresa tu nombre completo">
      </div>
      <div class="form-group">
        <label>Email:</label>
        <input type="email" placeholder="tu@email.com">
      </div>
      <div class="form-group">
        <label>Teléfono:</label>
        <input type="tel" placeholder="+1 234 567 8900">
      </div>
      <div class="form-group">
        <label>Mensaje:</label>
        <textarea rows="4" placeholder="Escribe tu mensaje aquí..."></textarea>
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn-secondary" onclick="closeModal('largeModal')">Cancelar</button>
      <button class="btn-primary">Enviar Formulario</button>
    </div>
  </div>
</div>

<div id="confirmModal" class="modal confirm">
  <div class="modal-backdrop" onclick="closeModal('confirmModal')"></div>
  <div class="modal-content">
    <div class="modal-header">
      <div class="confirm-icon">⚠️</div>
      <h3 class="modal-title">¿Confirmar acción?</h3>
      <button class="modal-close" onclick="closeModal('confirmModal')">×</button>
    </div>
    <div class="modal-body">
      <p>Esta acción no se puede deshacer. ¿Estás seguro de que quieres continuar?</p>
    </div>
    <div class="modal-footer">
      <button class="btn-secondary" onclick="closeModal('confirmModal')">Cancelar</button>
      <button class="btn-danger" onclick="closeModal('confirmModal')">Confirmar</button>
    </div>
  </div>
</div>

<div id="fullscreenModal" class="modal fullscreen">
  <div class="modal-backdrop" onclick="closeModal('fullscreenModal')"></div>
  <div class="modal-content">
    <div class="modal-header">
      <h3 class="modal-title">Modal Pantalla Completa</h3>
      <button class="modal-close" onclick="closeModal('fullscreenModal')">×</button>
    </div>
    <div class="modal-body">
      <h4>Contenido Extenso</h4>
      <p>Este modal ocupa toda la pantalla, ideal para contenido extenso como formularios complejos, editores, o visualización de datos.</p>
      <div class="content-grid">
        <div class="content-card">
          <h5>Sección 1</h5>
          <p>Contenido de la primera sección...</p>
        </div>
        <div class="content-card">
          <h5>Sección 2</h5>
          <p>Contenido de la segunda sección...</p>
        </div>
        <div class="content-card">
          <h5>Sección 3</h5>
          <p>Contenido de la tercera sección...</p>
        </div>
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn-secondary" onclick="closeModal('fullscreenModal')">Cerrar</button>
      <button class="btn-primary">Guardar Cambios</button>
    </div>
  </div>
</div>

<CodeViewer code={code} />

<script is:inline>
function openModal(modalId) {
  const modal = document.getElementById(modalId);
  if (modal) {
    modal.classList.add('show');
    document.body.style.overflow = 'hidden';
  }
}

function closeModal(modalId) {
  const modal = document.getElementById(modalId);
  if (modal) {
    modal.classList.remove('show');
    document.body.style.overflow = 'auto';
  }
}

// Cerrar modal con ESC
document.addEventListener('keydown', function(e) {
  if (e.key === 'Escape') {
    const openModal = document.querySelector('.modal.show');
    if (openModal) {
      openModal.classList.remove('show');
      document.body.style.overflow = 'auto';
    }
  }
});

// Make functions globally available
window.openModal = openModal;
window.closeModal = closeModal;
</script>

<style is:global>
  .modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .modal.show {
    opacity: 1;
    visibility: visible;
  }

  .modal-backdrop {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(4px);
  }

  .modal-content {
    background: var(--surface-color);
    border-radius: 16px;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    max-width: 500px;
    width: 90%;
    max-height: 90vh;
    overflow: hidden;
    transform: scale(0.9);
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    z-index: 1001;
    border: 1px solid var(--border-color);
  }

  .modal.show .modal-content {
    transform: scale(1);
  }

  .modal.large .modal-content {
    max-width: 800px;
  }

  .modal.confirm .modal-content {
    max-width: 400px;
  }

  .modal.fullscreen .modal-content {
    max-width: 95vw;
    max-height: 95vh;
    width: 95%;
    height: 95%;
  }

  .modal-header {
    padding: 1.5rem;
    border-bottom: 1px solid var(--border-color);
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
  }

  .modal.confirm .modal-header {
    flex-direction: column;
    text-align: center;
    gap: 0.5rem;
  }

  .confirm-icon {
    font-size: 3rem;
    margin-bottom: 0.5rem;
  }

  .modal-title {
    margin: 0;
    color: var(--text-color);
    font-size: 1.25rem;
    font-weight: 700;
    flex: 1;
  }

  .modal.confirm .modal-title {
    flex: none;
  }

  .modal-close {
    width: 32px;
    height: 32px;
    border: none;
    background: transparent;
    color: var(--text-secondary);
    font-size: 1.5rem;
    cursor: pointer;
    border-radius: 6px;
    transition: all 0.3s ease;
    flex-shrink: 0;
  }

  .modal.confirm .modal-close {
    position: absolute;
    top: 1rem;
    right: 1rem;
  }

  .modal-close:hover {
    background: var(--surface-secondary);
    color: var(--text-color);
    transform: scale(1.1);
  }

  .modal-body {
    padding: 1.5rem;
    overflow-y: auto;
    max-height: 60vh;
  }

  .modal.fullscreen .modal-body {
    max-height: calc(95vh - 140px);
  }

  .modal-footer {
    padding: 1.5rem;
    border-top: 1px solid var(--border-color);
    display: flex;
    gap: 0.75rem;
    justify-content: flex-end;
    flex-wrap: wrap;
  }

  /* Form Styles */
  .form-group {
    margin-bottom: 1.5rem;
  }

  .form-group label {
    display: block;
    margin-bottom: 0.5rem;
    color: var(--text-color);
    font-weight: 600;
    font-size: 0.9rem;
  }

  .form-group input,
  .form-group textarea {
    width: 100%;
    padding: 0.75rem;
    border: 1px solid var(--border-color);
    border-radius: 8px;
    background: var(--background-color);
    color: var(--text-color);
    font-size: 0.9rem;
    transition: all 0.3s ease;
  }

  .form-group input:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--primary-color);
    box-shadow: 0 0 0 3px rgba(var(--primary-color-rgb, 59, 130, 246), 0.1);
  }

  /* Button Styles */
  .modal-trigger,
  .btn-primary,
  .btn-secondary,
  .btn-danger {
    border: none;
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .modal-trigger,
  .btn-primary {
    background: var(--primary-color);
    color: white;
  }

  .modal-trigger:hover,
  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(var(--primary-color-rgb, 59, 130, 246), 0.3);
  }

  .btn-secondary {
    background: transparent;
    color: var(--text-secondary);
    border: 1px solid var(--border-color);
  }

  .btn-secondary:hover {
    background: var(--surface-secondary);
    color: var(--text-color);
    transform: translateY(-1px);
  }

  .btn-danger {
    background: #ef4444;
    color: white;
  }

  .btn-danger:hover {
    background: #dc2626;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(239, 68, 68, 0.3);
  }

  /* Content Grid for Fullscreen Modal */
  .content-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin-top: 1.5rem;
  }

  .content-card {
    background: var(--background-color);
    padding: 1.5rem;
    border-radius: 12px;
    border: 1px solid var(--border-color);
  }

  .content-card h5 {
    margin: 0 0 0.75rem 0;
    color: var(--text-color);
    font-size: 1.1rem;
    font-weight: 600;
  }

  .content-card p {
    margin: 0;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* Modal Showcase Layout */
  .modal-showcase {
    width: 100%;
    max-width: 600px;
  }

  .modal-section {
    margin-bottom: 2rem;
  }

  .modal-section h4 {
    margin: 0 0 1rem 0;
    color: var(--text-color);
    font-size: 1.1rem;
    font-weight: 600;
    padding-left: 0.5rem;
    border-left: 3px solid var(--primary-color);
  }

  .modal-controls {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
  }

  /* Component Preview Styles */
  .component-preview {
    padding: 2rem 1rem;
    border: 1px solid var(--border-color);
    border-radius: 16px;
    background: linear-gradient(135deg, var(--background-color) 0%, var(--surface-color) 100%);
    transition: all 0.3s ease;
  }

  .component-preview h3 {
    margin: 0 0 0.5rem 0;
    color: var(--text-color);
    font-size: 1.5rem;
    font-weight: 700;
    text-align: center;
    background: linear-gradient(135deg, var(--text-color), var(--primary-color));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .component-preview p {
    margin: 0 0 1.5rem 0;
    color: var(--text-secondary);
    line-height: 1.6;
    text-align: center;
    font-size: 1rem;
  }

  .demo-container {
    display: flex;
    justify-content: center;
    align-items: flex-start;
    min-height: 200px;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .modal-content {
      width: 95%;
      max-height: 95vh;
      margin: 1rem;
    }

    .modal.fullscreen .modal-content {
      width: 100%;
      height: 100%;
      max-width: 100vw;
      max-height: 100vh;
      border-radius: 0;
    }

    .modal-header,
    .modal-body,
    .modal-footer {
      padding: 1rem;
    }

    .modal-footer {
      flex-direction: column;
    }

    .modal-footer button {
      width: 100%;
    }

    .modal-controls {
      grid-template-columns: 1fr;
    }

    .content-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
