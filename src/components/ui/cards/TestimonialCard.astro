---
import CodeViewer from '../../util/CodeViewer.astro';

const code = `
<div class="testimonial-card">
  <div class="testimonial-header">
    <div class="testimonial-avatar">
      <img src="/api/placeholder/60/60" alt="Ana García" />
    </div>
    <div class="testimonial-author">
      <h5 class="author-name">Ana García</h5>
      <p class="author-role">CEO, TechCorp</p>
    </div>
    <div class="testimonial-rating">
      <div class="stars">
        <span class="star filled">★</span>
        <span class="star filled">★</span>
        <span class="star filled">★</span>
        <span class="star filled">★</span>
        <span class="star filled">★</span>
      </div>
    </div>
  </div>
  
  <div class="testimonial-content">
    <div class="quote-icon">"</div>
    <p class="testimonial-text">
      Este producto ha transformado completamente nuestro flujo de trabajo. 
      La interfaz es intuitiva y las características avanzadas nos han ahorrado 
      horas de trabajo cada semana.
    </p>
  </div>
  
  <div class="testimonial-footer">
    <span class="testimonial-date">Hace 2 semanas</span>
    <div class="testimonial-actions">
      <button class="action-btn">👍</button>
      <button class="action-btn">💬</button>
      <button class="action-btn">📤</button>
    </div>
  </div>
</div>

<div class="testimonial-card compact">
  <div class="testimonial-header">
    <div class="testimonial-avatar">
      <img src="/api/placeholder/50/50" alt="Carlos Ruiz" />
    </div>
    <div class="testimonial-author">
      <h5 class="author-name">Carlos Ruiz</h5>
      <p class="author-role">Desarrollador</p>
    </div>
    <div class="testimonial-rating">
      <div class="stars">
        <span class="star filled">★</span>
        <span class="star filled">★</span>
        <span class="star filled">★</span>
        <span class="star filled">★</span>
        <span class="star">★</span>
      </div>
    </div>
  </div>
  
  <div class="testimonial-content">
    <p class="testimonial-text">
      Excelente herramienta para desarrollo. Muy recomendada.
    </p>
  </div>
</div>

<style>
  .testimonial-card {
    background: var(--surface-color);
    border-radius: 20px;
    padding: 1.5rem;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1), 0 4px 16px rgba(0, 0, 0, 0.05);
    border: 1px solid var(--border-color);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    backdrop-filter: blur(10px);
    width: 100%;
    min-width: 280px;
    max-width: 100%;
  }

  .testimonial-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 4px;
    background: linear-gradient(90deg, #f59e0b, #d97706);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.3s ease;
  }

  .testimonial-card:hover::before {
    transform: scaleX(1);
  }

  .testimonial-card:hover {
    transform: translateY(-8px) scale(1.02);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15), 0 8px 32px rgba(0, 0, 0, 0.1);
  }

  .testimonial-card.compact {
    padding: 1.25rem;
    
  }

  .testimonial-card.compact::before {
    background: linear-gradient(90deg, var(--primary-color), var(--accent-color, var(--primary-color)));
  }

  .testimonial-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .testimonial-avatar {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    overflow: hidden;
    border: 3px solid var(--border-color);
    transition: all 0.3s ease;
    flex-shrink: 0;
  }

  .testimonial-card.compact .testimonial-avatar {
    width: 50px;
    height: 50px;
  }

  .testimonial-card:hover .testimonial-avatar {
    border-color: var(--primary-color);
    transform: scale(1.05);
  }

  .testimonial-avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .testimonial-author {
    flex: 1;
    min-width: 0;
  }

  .author-name {
    margin: 0 0 0.25rem 0;
    color: var(--text-color);
    font-size: 1.1rem;
    font-weight: 700;
    transition: color 0.3s ease;
  }

  .testimonial-card:hover .author-name {
    color: var(--primary-color);
  }

  .author-role {
    margin: 0;
    color: var(--text-secondary);
    font-size: 0.85rem;
    font-weight: 500;
  }

  .testimonial-rating {
    flex-shrink: 0;
  }

  .stars {
    display: flex;
    gap: 0.125rem;
  }

  .star {
    color: #e5e7eb;
    font-size: 1rem;
    transition: all 0.3s ease;
  }

  .star.filled {
    color: #f59e0b;
  }

  .testimonial-card:hover .star.filled {
    transform: scale(1.1);
    filter: drop-shadow(0 0 4px rgba(245, 158, 11, 0.5));
  }

  .testimonial-content {
    position: relative;
    margin-bottom: 1.5rem;
  }

  .testimonial-card.compact .testimonial-content {
    margin-bottom: 0;
  }

  .quote-icon {
    position: absolute;
    top: -0.5rem;
    left: -0.5rem;
    font-size: 3rem;
    color: var(--primary-color);
    opacity: 0.3;
    font-family: serif;
    line-height: 1;
  }

  .testimonial-text {
    margin: 0;
    color: var(--text-color);
    font-size: 0.95rem;
    line-height: 1.7;
    font-style: italic;
    position: relative;
    z-index: 1;
    transition: color 0.3s ease;
  }

  .testimonial-card.compact .testimonial-text {
    font-size: 0.9rem;
    font-style: normal;
  }

  .testimonial-card:hover .testimonial-text {
    color: var(--text-color);
  }

  .testimonial-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 1rem;
    border-top: 1px solid var(--border-color);
  }

  .testimonial-date {
    color: var(--text-secondary);
    font-size: 0.8rem;
    font-weight: 500;
  }

  .testimonial-actions {
    display: flex;
    gap: 0.5rem;
  }

  .action-btn {
    width: 32px;
    height: 32px;
    border: none;
    border-radius: 8px;
    background: var(--surface-secondary);
    border: 1px solid var(--border-color);
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.9rem;
  }

  .action-btn:hover {
    background: var(--primary-color);
    border-color: var(--primary-color);
    transform: translateY(-1px) scale(1.1);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .testimonial-card {
      max-width: 100%;
      min-width: 0;
      padding: 1.25rem;
    }

    .testimonial-header {
      gap: 0.75rem;
      margin-bottom: 1.25rem;
    }

    .testimonial-avatar {
      width: 50px;
      height: 50px;
    }

    .author-name {
      font-size: 1rem;
    }

    .testimonial-text {
      font-size: 0.9rem;
    }

    .quote-icon {
      font-size: 2.5rem;
    }
  }
</style>
`;
---

<div class="component-preview">
  <h3>Tarjetas de Testimonios</h3>
  <p>Tarjetas para mostrar reseñas y testimonios de usuarios con calificaciones y acciones.</p>
  
  <div class="demo-container">
    <div class="testimonial-showcase">
      <div class="testimonial-card">
        <div class="testimonial-header">
          <div class="testimonial-avatar">
            <div class="avatar-placeholder">👩‍💼</div>
          </div>
          <div class="testimonial-author">
            <h5 class="author-name">Ana García</h5>
            <p class="author-role">CEO, TechCorp</p>
          </div>
          <div class="testimonial-rating">
            <div class="stars">
              <span class="star filled">★</span>
              <span class="star filled">★</span>
              <span class="star filled">★</span>
              <span class="star filled">★</span>
              <span class="star filled">★</span>
            </div>
          </div>
        </div>
        
        <div class="testimonial-content">
          <div class="quote-icon">"</div>
          <p class="testimonial-text">
            Este producto ha transformado completamente nuestro flujo de trabajo. 
            La interfaz es intuitiva y las características avanzadas nos han ahorrado 
            horas de trabajo cada semana.
          </p>
        </div>
        
        <div class="testimonial-footer">
          <span class="testimonial-date">Hace 2 semanas</span>
          <div class="testimonial-actions">
            <button class="action-btn">👍</button>
            <button class="action-btn">💬</button>
            <button class="action-btn">📤</button>
          </div>
        </div>
      </div>

      <div class="testimonial-card compact">
        <div class="testimonial-header">
          <div class="testimonial-avatar">
            <div class="avatar-placeholder">👨‍💻</div>
          </div>
          <div class="testimonial-author">
            <h5 class="author-name">Carlos Ruiz</h5>
            <p class="author-role">Desarrollador</p>
          </div>
          <div class="testimonial-rating">
            <div class="stars">
              <span class="star filled">★</span>
              <span class="star filled">★</span>
              <span class="star filled">★</span>
              <span class="star filled">★</span>
              <span class="star">★</span>
            </div>
          </div>
        </div>
        
        <div class="testimonial-content">
          <p class="testimonial-text">
            Excelente herramienta para desarrollo. Muy recomendada para equipos ágiles.
          </p>
        </div>
      </div>
    </div>
  </div>
</div>

<CodeViewer code={code} />

<style>
  /* Estilos duplicados para el funcionamiento del preview */
  .testimonial-card {
    background: var(--surface-color);
    border-radius: 20px;
    padding: 1.5rem;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1), 0 4px 16px rgba(0, 0, 0, 0.05);
    border: 1px solid var(--border-color);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    backdrop-filter: blur(10px);
    width: 100%;
    min-width: 280px;
    max-width: 100%;
  }

  .testimonial-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 4px;
    background: linear-gradient(90deg, #f59e0b, #d97706);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.3s ease;
  }

  .testimonial-card:hover::before {
    transform: scaleX(1);
  }

  .testimonial-card:hover {
    transform: translateY(-8px) scale(1.02);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15), 0 8px 32px rgba(0, 0, 0, 0.1);
  }

  .testimonial-card.compact {
    padding: 1.25rem;
    
  }

  .testimonial-card.compact::before {
    background: linear-gradient(90deg, var(--primary-color), var(--accent-color, var(--primary-color)));
  }

  .testimonial-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .testimonial-avatar {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    overflow: hidden;
    border: 3px solid var(--border-color);
    transition: all 0.3s ease;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--primary-color), var(--accent-color, var(--primary-color)));
  }

  .testimonial-card.compact .testimonial-avatar {
    width: 50px;
    height: 50px;
  }

  .testimonial-card:hover .testimonial-avatar {
    border-color: var(--primary-color);
    transform: scale(1.05);
  }

  .avatar-placeholder {
    font-size: 1.5rem;
    filter: brightness(0) invert(1);
  }

  .testimonial-card.compact .avatar-placeholder {
    font-size: 1.25rem;
  }

  .testimonial-author {
    flex: 1;
    min-width: 0;
  }

  .author-name {
    margin: 0 0 0.25rem 0;
    color: var(--text-color);
    font-size: 1.1rem;
    font-weight: 700;
    transition: color 0.3s ease;
  }

  .testimonial-card:hover .author-name {
    color: var(--primary-color);
  }

  .author-role {
    margin: 0;
    color: var(--text-secondary);
    font-size: 0.85rem;
    font-weight: 500;
  }

  .testimonial-rating {
    flex-shrink: 0;
  }

  .stars {
    display: flex;
    gap: 0.125rem;
  }

  .star {
    color: #e5e7eb;
    font-size: 1rem;
    transition: all 0.3s ease;
  }

  .star.filled {
    color: #f59e0b;
  }

  .testimonial-card:hover .star.filled {
    transform: scale(1.1);
    filter: drop-shadow(0 0 4px rgba(245, 158, 11, 0.5));
  }

  .testimonial-content {
    position: relative;
    margin-bottom: 1.5rem;
  }

  .testimonial-card.compact .testimonial-content {
    margin-bottom: 0;
  }

  .quote-icon {
    position: absolute;
    top: -0.5rem;
    left: -0.5rem;
    font-size: 3rem;
    color: var(--primary-color);
    opacity: 0.3;
    font-family: serif;
    line-height: 1;
  }

  .testimonial-text {
    margin: 0;
    color: var(--text-color);
    font-size: 0.95rem;
    line-height: 1.7;
    font-style: italic;
    position: relative;
    z-index: 1;
    transition: color 0.3s ease;
  }

  .testimonial-card.compact .testimonial-text {
    font-size: 0.9rem;
    font-style: normal;
  }

  .testimonial-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 1rem;
    border-top: 1px solid var(--border-color);
  }

  .testimonial-date {
    color: var(--text-secondary);
    font-size: 0.8rem;
    font-weight: 500;
  }

  .testimonial-actions {
    display: flex;
    gap: 0.5rem;
  }

  .action-btn {
    width: 32px;
    height: 32px;
    border: none;
    border-radius: 8px;
    background: var(--surface-secondary);
    border: 1px solid var(--border-color);
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.9rem;
  }

  .action-btn:hover {
    background: var(--primary-color);
    border-color: var(--primary-color);
    transform: translateY(-1px) scale(1.1);
  }

  @media (max-width: 768px) {
    .testimonial-card {
      max-width: 100%;
      min-width: 0;
      padding: 1.25rem;
    }

    .testimonial-header {
      gap: 0.75rem;
      margin-bottom: 1.25rem;
    }

    .testimonial-avatar {
      width: 50px;
      height: 50px;
    }

    .author-name {
      font-size: 1rem;
    }

    .testimonial-text {
      font-size: 0.9rem;
    }

    .quote-icon {
      font-size: 2.5rem;
    }
  }

  /* Estilos del contenedor de preview */
  .component-preview {
    padding: 2rem 1rem;
    border: 1px solid var(--border-color);
    border-radius: 16px;
    background: linear-gradient(135deg, var(--background-color) 0%, var(--surface-color) 100%);
    transition: all 0.3s ease;
  }

  .component-preview h3 {
    margin: 0 0 0.5rem 0;
    color: var(--text-color);
    font-size: 1.5rem;
    font-weight: 700;
    text-align: center;
    background: linear-gradient(135deg, var(--text-color), var(--primary-color));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .component-preview p {
    margin: 0 0 1.5rem 0;
    color: var(--text-secondary);
    line-height: 1.6;
    text-align: center;
    font-size: 1rem;
  }

  .demo-container {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 300px;
  }

  .testimonial-showcase {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    max-width: 100%;
    width: 100%;
  }

  @media (max-width: 768px) {
    .testimonial-showcase {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
  }
</style>
