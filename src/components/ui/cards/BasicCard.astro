---
import CodeViewer from '../../util/CodeViewer.astro';

const code = `
<div class="card-basic">
  <div class="card-header">
    <h4>Título de la Tarjeta</h4>
  </div>
  <div class="card-body">
    <p>Esta es una tarjeta básica con contenido simple. Incluye un título y descripción con un diseño limpio y moderno.</p>
    <button class="card-btn">Leer más</button>
  </div>
</div>

<style>
  .card-basic {
    background: var(--surface-color);
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08), 0 2px 6px rgba(0, 0, 0, 0.04);
    border: 1px solid var(--border-color);
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    max-width: 320px;
    position: relative;
    backdrop-filter: blur(10px);
  }

  .card-basic::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--primary-color), var(--accent-color, var(--primary-color)));
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .card-basic:hover::before {
    opacity: 1;
  }

  html.dark .card-basic {
    background: var(--surface-color);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2), 0 2px 6px rgba(0, 0, 0, 0.1);
    border-color: var(--border-color);
  }

  .card-basic:hover {
    transform: translateY(-8px) scale(1.02);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15), 0 6px 16px rgba(0, 0, 0, 0.08);
    border-color: var(--primary-color);
  }

  html.dark .card-basic:hover {
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.4), 0 6px 16px rgba(0, 0, 0, 0.2);
  }

  .card-header {
    padding: 2rem 2rem 0;
    position: relative;
  }

  .card-header::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 2rem;
    width: 30px;
    height: 2px;
    background: var(--primary-color);
    border-radius: 1px;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.3s ease;
  }

  .card-basic:hover .card-header::after {
    transform: scaleX(1);
  }

  .card-header h4 {
    margin: 0;
    color: var(--text-color);
    font-size: 1.4rem;
    font-weight: 700;
    letter-spacing: -0.025em;
    line-height: 1.3;
    transition: color 0.3s ease;
  }

  .card-basic:hover .card-header h4 {
    color: var(--primary-color);
  }

  .card-body {
    padding: 1.5rem 2rem 2rem;
  }

  .card-body p {
    color: var(--text-secondary);
    line-height: 1.7;
    margin: 0 0 2rem 0;
    font-size: 0.95rem;
    transition: color 0.3s ease;
  }

  .card-basic:hover .card-body p {
    color: var(--text-color);
  }

  .card-btn {
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--accent-color, var(--primary-color)) 100%);
    color: white;
    border: none;
    padding: 12px 24px;
    border-radius: 10px;
    font-size: 0.9rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    min-width: 120px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    text-transform: capitalize;
    letter-spacing: 0.025em;
  }

  .card-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
    transition: left 0.5s;
  }

  .card-btn::after {
    content: '→';
    position: absolute;
    right: 12px;
    transform: translateX(20px);
    opacity: 0;
    transition: all 0.3s ease;
  }

  .card-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(var(--primary-color-rgb, 59, 130, 246), 0.4);
    padding-right: 36px;
  }

  .card-btn:hover::before {
    left: 100%;
  }

  .card-btn:hover::after {
    transform: translateX(0);
    opacity: 1;
  }

  .card-btn:active {
    transform: translateY(0);
  }

  .card-btn:focus-visible {
    outline: 2px solid var(--primary-color);
    outline-offset: 2px;
  }

  /* Animación de entrada */
  .card-basic {
    animation: cardFadeIn 0.6s ease-out;
  }

  @keyframes cardFadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .card-basic {
      max-width: 100%;
      margin: 0 auto;
    }

    .card-header {
      padding: 1.5rem 1.5rem 0;
    }

    .card-header::after {
      left: 1.5rem;
    }

    .card-body {
      padding: 1rem 1.5rem 1.5rem;
    }

    .card-btn {
      padding: 10px 20px;
      font-size: 0.85rem;
      min-width: 100px;
    }

    .card-btn:hover {
      padding-right: 32px;
    }
  }

  /* Estados adicionales para interactividad */
  .card-basic:focus-within {
    outline: 2px solid var(--primary-color);
    outline-offset: 2px;
  }

  /* Efecto de pulso sutil */
  .card-basic:hover {
    animation: subtleGlow 2s infinite;
  }

  @keyframes subtleGlow {
    0%, 100% {
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15), 0 6px 16px rgba(0, 0, 0, 0.08);
    }
    50% {
      box-shadow: 0 12px 40px rgba(var(--primary-color-rgb, 59, 130, 246), 0.1), 0 6px 16px rgba(0, 0, 0, 0.08);
    }
  }

  html.dark .card-basic:hover {
    animation: subtleGlowDark 2s infinite;
  }

  @keyframes subtleGlowDark {
    0%, 100% {
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.4), 0 6px 16px rgba(0, 0, 0, 0.2);
    }
    50% {
      box-shadow: 0 12px 40px rgba(var(--primary-color-rgb, 59, 130, 246), 0.2), 0 6px 16px rgba(0, 0, 0, 0.2);
    }
  }
</style>
`;
---

<div class="component-preview">
  <div class="card-basic">
    <div class="card-header">
      <h4>Título de la Tarjeta</h4>
    </div>
    <div class="card-body">
      <p>Esta es una tarjeta básica con contenido simple. Incluye un título y descripción con un diseño limpio y moderno.</p>
      <button class="card-btn">Leer más</button>
    </div>
  </div>
</div>

<CodeViewer code={code} />

<style>
  /* Solo los estilos del componente card-basic están duplicados para funcionar en el preview */
  .card-basic {
    background: var(--surface-color);
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08), 0 2px 6px rgba(0, 0, 0, 0.04);
    border: 1px solid var(--border-color);
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    max-width: 320px;
    position: relative;
    backdrop-filter: blur(10px);
  }

  .card-basic::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--primary-color), var(--accent-color, var(--primary-color)));
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .card-basic:hover::before {
    opacity: 1;
  }

  html.dark .card-basic {
    background: var(--surface-color);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2), 0 2px 6px rgba(0, 0, 0, 0.1);
    border-color: var(--border-color);
  }

  .card-basic:hover {
    transform: translateY(-8px) scale(1.02);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15), 0 6px 16px rgba(0, 0, 0, 0.08);
    border-color: var(--primary-color);
  }

  html.dark .card-basic:hover {
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.4), 0 6px 16px rgba(0, 0, 0, 0.2);
  }

  .card-header {
    padding: 2rem 2rem 0;
    position: relative;
  }

  .card-header::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 2rem;
    width: 30px;
    height: 2px;
    background: var(--primary-color);
    border-radius: 1px;
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.3s ease;
  }

  .card-basic:hover .card-header::after {
    transform: scaleX(1);
  }

  .card-header h4 {
    margin: 0;
    color: var(--text-color);
    font-size: 1.4rem;
    font-weight: 700;
    letter-spacing: -0.025em;
    line-height: 1.3;
    transition: color 0.3s ease;
  }

  .card-basic:hover .card-header h4 {
    color: var(--primary-color);
  }

  .card-body {
    padding: 1.5rem 2rem 2rem;
  }

  .card-body p {
    color: var(--text-secondary);
    line-height: 1.7;
    margin: 0 0 2rem 0;
    font-size: 0.95rem;
    transition: color 0.3s ease;
  }

  .card-basic:hover .card-body p {
    color: var(--text-color);
  }

  .card-btn {
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--accent-color, var(--primary-color)) 100%);
    color: white;
    border: none;
    padding: 12px 24px;
    border-radius: 10px;
    font-size: 0.9rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    min-width: 120px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    text-transform: capitalize;
    letter-spacing: 0.025em;
  }

  .card-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
    transition: left 0.5s;
  }

  .card-btn::after {
    content: '→';
    position: absolute;
    right: 12px;
    transform: translateX(20px);
    opacity: 0;
    transition: all 0.3s ease;
  }

  .card-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(var(--primary-color-rgb, 59, 130, 246), 0.4);
    padding-right: 36px;
  }

  .card-btn:hover::before {
    left: 100%;
  }

  .card-btn:hover::after {
    transform: translateX(0);
    opacity: 1;
  }

  .card-btn:active {
    transform: translateY(0);
  }

  .card-btn:focus-visible {
    outline: 2px solid var(--primary-color);
    outline-offset: 2px;
  }

  .card-basic {
    animation: cardFadeIn 0.6s ease-out;
  }

  @keyframes cardFadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 768px) {
    .card-basic {
      max-width: 100%;
      margin: 0 auto;
    }

    .card-header {
      padding: 1.5rem 1.5rem 0;
    }

    .card-header::after {
      left: 1.5rem;
    }

    .card-body {
      padding: 1rem 1.5rem 1.5rem;
    }

    .card-btn {
      padding: 10px 20px;
      font-size: 0.85rem;
      min-width: 100px;
    }

    .card-btn:hover {
      padding-right: 32px;
    }
  }

  .card-basic:focus-within {
    outline: 2px solid var(--primary-color);
    outline-offset: 2px;
  }

  .card-basic:hover {
    animation: subtleGlow 2s infinite;
  }

  @keyframes subtleGlow {
    0%, 100% {
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15), 0 6px 16px rgba(0, 0, 0, 0.08);
    }
    50% {
      box-shadow: 0 12px 40px rgba(var(--primary-color-rgb, 59, 130, 246), 0.1), 0 6px 16px rgba(0, 0, 0, 0.08);
    }
  }

  html.dark .card-basic:hover {
    animation: subtleGlowDark 2s infinite;
  }

  @keyframes subtleGlowDark {
    0%, 100% {
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.4), 0 6px 16px rgba(0, 0, 0, 0.2);
    }
    50% {
      box-shadow: 0 12px 40px rgba(var(--primary-color-rgb, 59, 130, 246), 0.2), 0 6px 16px rgba(0, 0, 0, 0.2);
    }
  }

  /* Estilos del contenedor de preview */
  .component-preview {
    padding: 3rem 2rem;
    border: 1px solid var(--border-color);
    border-radius: 12px;
    background: var(--background-color);
    transition: all 0.3s ease;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 400px;
  }
  
</style>
