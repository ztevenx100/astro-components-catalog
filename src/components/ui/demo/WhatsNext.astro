---
const nextSteps = [
  {
    title: '🧪 Prueba los Componentes',
    description: 'Interactúa con todos los botones, prueba diferentes estados y variantes.',
    action: 'Explorar'
  },
  {
    title: '📋 Copia el Código',
    description: 'Usa los ejemplos de código para implementar en tus proyectos.',
    action: 'Implementar'
  },
  {
    title: '🎨 Personaliza el Diseño',
    description: 'Modifica las variables CSS para adaptar el sistema a tu marca.',
    action: 'Customizar'
  },
  {
    title: '🚀 Mejora el Sistema',
    description: 'Sugiere nuevos patrones o mejoras para el sistema de botones.',
    action: 'Contribuir'
  }
];
---

<div class="whats-next">
  <div class="whats-next-header">
    <h3>🎯 ¿Qué Sigue?</h3>
    <p>Aprovecha al máximo el sistema de botones</p>
  </div>

  <div class="next-steps-grid">
    {nextSteps.map((step, index) => (
      <div class="next-step">
        <div class="step-number">{index + 1}</div>
        <div class="step-content">
          <h4>{step.title}</h4>
          <p>{step.description}</p>
          <div class="step-action">{step.action}</div>
        </div>
      </div>
    ))}
  </div>

  <div class="quick-access">
    <h4>⚡ Acceso Rápido</h4>
    <div class="quick-buttons">
      <a href="#button-system" class="btn btn-primary btn-sm">
        📖 Documentación Base
      </a>
      <a href="#gradients" class="btn btn-success btn-sm">
        🌈 Ver Gradientes
      </a>
      <a href="#icons" class="btn btn-warning btn-sm">
        🎯 Iconos & Animaciones
      </a>
      <a href="#forms" class="btn btn-info btn-sm">
        📝 Patrones de Formularios
      </a>
    </div>
  </div>
</div>

<style>
  .whats-next {
    background: linear-gradient(135deg, var(--surface-color) 0%, var(--background-color) 100%);
    border-radius: 16px;
    padding: 2rem;
    margin: 2rem 0;
    border: 1px solid var(--border-color);
    position: relative;
    overflow: hidden;
  }

  .whats-next::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, 
      var(--primary-color) 0%, 
      var(--success-color) 25%,
      var(--warning-color) 50%,
      var(--info-color) 75%,
      var(--primary-color) 100%
    );
  }

  .whats-next-header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .whats-next-header h3 {
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--text-color);
    margin: 0 0 0.5rem 0;
  }

  .whats-next-header p {
    color: var(--text-secondary);
    margin: 0;
    font-size: 1rem;
  }

  .next-steps-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin-bottom: 2rem;
  }

  .next-step {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1.5rem;
    background: var(--background-color);
    border: 1px solid var(--border-color);
    border-radius: 12px;
    transition: all 0.3s ease;
    position: relative;
  }

  .next-step:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(var(--primary-color-rgb), 0.15);
    border-color: var(--primary-color);
  }

  .step-number {
    flex-shrink: 0;
    width: 32px;
    height: 32px;
    background: var(--primary-color);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    font-size: 0.9rem;
  }

  .step-content h4 {
    margin: 0 0 0.5rem 0;
    color: var(--text-color);
    font-size: 1rem;
    font-weight: 600;
  }

  .step-content p {
    margin: 0 0 0.75rem 0;
    color: var(--text-secondary);
    font-size: 0.9rem;
    line-height: 1.4;
  }

  .step-action {
    background: var(--surface-color);
    color: var(--primary-color);
    padding: 0.25rem 0.75rem;
    border-radius: 6px;
    font-size: 0.8rem;
    font-weight: 500;
    display: inline-block;
    border: 1px solid var(--primary-color);
  }

  .quick-access {
    background: var(--surface-color);
    border: 1px solid var(--border-color);
    border-radius: 12px;
    padding: 1.5rem;
    text-align: center;
  }

  .quick-access h4 {
    margin: 0 0 1rem 0;
    color: var(--text-color);
    font-size: 1.1rem;
  }

  .quick-buttons {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .quick-buttons .btn {
    text-decoration: none;
    min-width: 140px;
  }

  @media (max-width: 768px) {
    .whats-next {
      padding: 1.5rem;
    }

    .next-steps-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .next-step {
      padding: 1.25rem;
    }

    .quick-buttons {
      flex-direction: column;
      align-items: center;
    }

    .quick-buttons .btn {
      width: 100%;
      max-width: 200px;
    }
  }
</style>
