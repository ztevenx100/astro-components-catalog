---
import CodeViewer from '../../util/CodeViewer.astro';

const code = `
<!-- Hero Section -->
<section class="hero-section">
  <div class="hero-content">
    <h1 class="hero-title">Título Principal</h1>
    <p class="hero-subtitle">Subtítulo descriptivo del contenido</p>
    <div class="hero-actions">
      <button class="btn btn-primary">Acción Principal</button>
      <button class="btn btn-outline-secondary">Acción Secundaria</button>
    </div>
  </div>
  <div class="hero-visual">
    <div class="hero-placeholder">Visual</div>
  </div>
</section>

<!-- Sidebar Layout -->
<div class="sidebar-layout">
  <aside class="sidebar">
    <nav class="sidebar-nav">
      <a href="#" class="nav-item active">Dashboard</a>
      <a href="#" class="nav-item">Usuarios</a>
      <a href="#" class="nav-item">Configuración</a>
    </nav>
  </aside>
  <main class="main-content">
    <header class="content-header">
      <h2>Dashboard Principal</h2>
      <p>Contenido principal del layout</p>
    </header>
    <div class="content-body">
      <p>Área de contenido principal con sidebar lateral.</p>
    </div>
  </main>
</div>

<!-- Dashboard Grid -->
<div class="dashboard-grid">
  <div class="dashboard-card stat-card">
    <h3>Usuarios</h3>
    <div class="stat-number">1,234</div>
  </div>
  <div class="dashboard-card stat-card">
    <h3>Ventas</h3>
    <div class="stat-number">$45,678</div>
  </div>
  <div class="dashboard-card chart-card">
    <h3>Gráfico</h3>
    <div class="chart-placeholder">📊</div>
  </div>
  <div class="dashboard-card table-card">
    <h3>Tabla de Datos</h3>
    <div class="table-placeholder">📋</div>
  </div>
</div>

<style>
/* ===== HERO SECTION ===== */
.hero-section {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 3rem;
  align-items: center;
  padding: 4rem 2rem;
  background: linear-gradient(135deg, var(--primary-color) 0%, var(--info-color) 100%);
  color: white;
  border-radius: 16px;
  margin-bottom: 2rem;
  overflow: hidden;
  box-sizing: border-box;
  width: 100%;
}

.hero-content h1 {
  font-size: 3rem;
  font-weight: 700;
  margin: 0 0 1rem 0;
  line-height: 1.2;
  word-wrap: break-word;
  overflow-wrap: break-word;
}

.hero-content p {
  font-size: 1.3rem;
  margin: 0 0 2rem 0;
  opacity: 0.9;
  line-height: 1.4;
  word-wrap: break-word;
}

.hero-actions {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.hero-visual {
  display: flex;
  justify-content: center;
  align-items: center;
}

.hero-placeholder {
  width: 200px;
  height: 200px;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.2rem;
  font-weight: 600;
}

/* ===== SIDEBAR LAYOUT ===== */
.sidebar-layout {
  display: grid;
  grid-template-columns: 250px 1fr;
  gap: 0;
  min-height: 400px;
  border: 1px solid var(--border-color);
  border-radius: 12px;
  overflow: hidden;
  background: var(--surface-color);
  margin-bottom: 2rem;
}

.sidebar {
  background: var(--surface-secondary);
  padding: 1.5rem 0;
  border-right: 1px solid var(--border-color);
}

.sidebar-nav {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
}

.nav-item {
  display: block;
  padding: 0.75rem 1.5rem;
  color: var(--text-secondary);
  text-decoration: none;
  transition: all 0.3s ease;
  font-weight: 500;
}

.nav-item:hover {
  background: var(--surface-color);
  color: var(--text-color);
}

.nav-item.active {
  background: var(--primary-color);
  color: white;
}

.main-content {
  padding: 2rem;
}

.content-header h2 {
  margin: 0 0 0.5rem 0;
  color: var(--text-color);
  font-size: 1.5rem;
}

.content-header p {
  margin: 0 0 1.5rem 0;
  color: var(--text-secondary);
}

.content-body {
  color: var(--text-color);
  line-height: 1.6;
}

/* ===== DASHBOARD GRID ===== */
.dashboard-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 1.5rem;
  margin-bottom: 2rem;
  width: 100%;
  box-sizing: border-box;
}

.dashboard-card {
  padding: 1.5rem;
  background: var(--surface-color);
  border: 1px solid var(--border-color);
  border-radius: 12px;
  transition: all 0.3s ease;
  min-width: 0; /* Permite que el contenido se encoja */
  word-wrap: break-word;
}
.dashboard-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
}

.dashboard-card h3 {
  margin: 0 0 1rem 0;
  color: var(--text-color);
  font-size: 1.1rem;
  font-weight: 600;
}

.stat-card .stat-number {
  font-size: 2.5rem;
  font-weight: 700;
  color: var(--primary-color);
  margin: 0;
}

.chart-card,
.table-card {
  min-height: 150px;
}

.chart-placeholder,
.table-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100px;
  background: var(--surface-secondary);
  border-radius: 8px;
  font-size: 2rem;
  border: 2px dashed var(--border-color);
}

/* Responsive */
@media (max-width: 1024px) {
  .sidebar-layout {
    grid-template-columns: 200px 1fr;
  }
}

@media (max-width: 768px) {
  .hero-section {
    grid-template-columns: 1fr;
    text-align: center;
    padding: 3rem 1.5rem;
  }
  
  .hero-content h1 {
    font-size: 2.2rem;
  }
  
  .hero-content p {
    font-size: 1.1rem;
  }
  
  .sidebar-layout {
    grid-template-columns: 1fr;
  }
  
  .sidebar {
    border-right: none;
    border-bottom: 1px solid var(--border-color);
  }
  
  .sidebar-nav {
    flex-direction: row;
    overflow-x: auto;
    padding: 0 1rem;
  }
  
  .nav-item {
    white-space: nowrap;
    padding: 0.75rem 1rem;
  }
  
  .dashboard-grid {
    grid-template-columns: 1fr;
    gap: 1rem;
  }
}

@media (max-width: 480px) {
  .hero-section {
    padding: 2rem 1rem;
  }
  
  .hero-content h1 {
    font-size: 1.8rem;
  }
  
  .hero-actions {
    flex-direction: column;
  }
  
  .hero-placeholder {
    width: 150px;
    height: 150px;
  }
  
  .main-content {
    padding: 1.5rem;
  }
}
</style>
`;
---

<div class="component-preview">
  <h3>🏢 Layouts Especializados</h3>
  <p>Layouts prediseñados para secciones específicas como héroes, dashboards y sidebars.</p>
  
  <div class="demo-container">
    <div class="layouts-showcase">
      
      <!-- Hero Section -->
      <div class="demo-section">
        <h4>🌟 Hero Section</h4>
        <section class="hero-section">
          <div class="hero-content">
            <h1 class="hero-title">Bienvenido al Sistema</h1>
            <p class="hero-subtitle">La mejor plataforma para gestionar tus proyectos</p>
            <div class="hero-actions">
              <button class="btn btn-primary">Comenzar</button>
              <button class="btn btn-outline-light">Más Info</button>
            </div>
          </div>
          <div class="hero-visual">
            <div class="hero-placeholder">🚀</div>
          </div>
        </section>
      </div>

      <!-- Sidebar Layout -->
      <div class="demo-section">
        <h4>📱 Sidebar Layout</h4>
        <div class="sidebar-layout">
          <aside class="sidebar">
            <nav class="sidebar-nav">
              <a href="#" class="nav-item active">📊 Dashboard</a>
              <a href="#" class="nav-item">👥 Usuarios</a>
              <a href="#" class="nav-item">📈 Reportes</a>
              <a href="#" class="nav-item">⚙️ Configuración</a>
            </nav>
          </aside>
          <main class="main-content">
            <header class="content-header">
              <h2>Dashboard Principal</h2>
              <p>Contenido principal con navegación lateral</p>
            </header>
            <div class="content-body">
              <p>Esta es el área principal de contenido. El sidebar se colapsa automáticamente en dispositivos móviles para mantener una experiencia de usuario óptima.</p>
            </div>
          </main>
        </div>
      </div>

      <!-- Dashboard Grid -->
      <div class="demo-section">
        <h4>📊 Dashboard Grid</h4>
        <div class="dashboard-grid">
          <div class="dashboard-card stat-card">
            <h3>👥 Usuarios Activos</h3>
            <div class="stat-number">1,234</div>
          </div>
          <div class="dashboard-card stat-card">
            <h3>💰 Ingresos</h3>
            <div class="stat-number">$45,678</div>
          </div>
          <div class="dashboard-card chart-card">
            <h3>📈 Gráfico de Tendencias</h3>
            <div class="chart-placeholder">📊</div>
          </div>
          <div class="dashboard-card table-card">
            <h3>📋 Datos Recientes</h3>
            <div class="table-placeholder">📄</div>
          </div>
        </div>
      </div>

    </div>
  </div>

</div>

<CodeViewer code={code} />

<style>
  .component-preview {
    padding: 2rem;
    border: 1px solid var(--border-color);
    border-radius: 16px;
    background: linear-gradient(135deg, var(--background-color) 0%, var(--surface-color) 100%);
    margin-bottom: 2rem;
    overflow: hidden;
    box-sizing: border-box;
    width: 100%;
  }

  .component-preview h3 {
    margin: 0 0 0.5rem 0;
    color: var(--text-color);
    font-size: 1.6rem;
    font-weight: 700;
    text-align: center;
    background: linear-gradient(135deg, var(--text-color), var(--primary-color));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .component-preview p {
    margin: 0 0 2rem 0;
    color: var(--text-secondary);
    line-height: 1.6;
    text-align: center;
    font-size: 1rem;
  }

  .demo-container {
    display: flex;
    justify-content: center;
    align-items: flex-start;
    margin-bottom: 2rem;
  }

  .layouts-showcase {
    width: 100%;
    max-width: 1000px;
    display: flex;
    flex-direction: column;
    gap: 3rem;
    overflow: hidden;
    box-sizing: border-box;
  }

  .demo-section {
    width: 100%;
    overflow: hidden;
    box-sizing: border-box;
  }

  .demo-section h4 {
    margin: 0 0 1.5rem 0;
    color: var(--text-color);
    font-size: 1.2rem;
    font-weight: 600;
    text-align: center;
  }

  /* Button styles for hero */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.75rem 1.5rem;
    border: 2px solid transparent;
    border-radius: 8px;
    font-weight: 600;
    font-size: 0.875rem;
    text-decoration: none;
    cursor: pointer;
    transition: all 0.3s ease;
    min-height: 44px;
  }

  .btn-primary {
    background: white;
    color: var(--primary-color);
    border-color: white;
  }

  .btn-primary:hover {
    background: rgba(255, 255, 255, 0.9);
    transform: translateY(-1px);
  }

  .btn-outline-light {
    background: transparent;
    color: white;
    border-color: rgba(255, 255, 255, 0.5);
  }

  .btn-outline-light:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: white;
  }

  /* Hero Section */
  .hero-section {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
    padding: 4rem 2rem;
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--info-color) 100%);
    color: white;
    border-radius: 16px;
    margin-bottom: 2rem;
  }

  .hero-content h1 {
    font-size: 2.5rem;
    font-weight: 700;
    margin: 0 0 1rem 0;
    line-height: 1.2;
  }

  .hero-content p {
    font-size: 1.2rem;
    margin: 0 0 2rem 0;
    opacity: 0.9;
    line-height: 1.4;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero-visual {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero-placeholder {
    width: 150px;
    height: 150px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 3rem;
    backdrop-filter: blur(10px);
  }

  /* Sidebar Layout */
  .sidebar-layout {
    display: grid;
    grid-template-columns: 250px 1fr;
    gap: 0;
    min-height: 350px;
    border: 1px solid var(--border-color);
    border-radius: 12px;
    overflow: hidden;
    background: var(--surface-color);
    margin-bottom: 2rem;
  }

  .sidebar {
    background: var(--surface-secondary);
    padding: 1.5rem 0;
    border-right: 1px solid var(--border-color);
  }

  .sidebar-nav {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .nav-item {
    display: block;
    padding: 0.75rem 1.5rem;
    color: var(--text-secondary);
    text-decoration: none;
    transition: all 0.3s ease;
    font-weight: 500;
  }

  .nav-item:hover {
    background: var(--surface-color);
    color: var(--text-color);
  }

  .nav-item.active {
    background: var(--primary-color);
    color: white;
  }

  .main-content {
    padding: 2rem;
  }

  .content-header h2 {
    margin: 0 0 0.5rem 0;
    color: var(--text-color);
    font-size: 1.5rem;
  }

  .content-header p {
    margin: 0 0 1.5rem 0;
    color: var(--text-secondary);
  }

  .content-body {
    color: var(--text-color);
    line-height: 1.6;
  }

  /* Dashboard Grid */
  .dashboard-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin-bottom: 2rem;
  }

  .dashboard-card {
    padding: 1.5rem;
    background: var(--surface-color);
    border: 1px solid var(--border-color);
    border-radius: 12px;
    transition: all 0.3s ease;
  }

  .dashboard-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
    border-color: var(--primary-color);
  }

  .dashboard-card h3 {
    margin: 0 0 1rem 0;
    color: var(--text-color);
    font-size: 1.1rem;
    font-weight: 600;
  }

  .stat-card .stat-number {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--primary-color);
    margin: 0;
    line-height: 1;
    word-break: break-all; /* Permite quebrar números largos */
  }

  .chart-card,
  .table-card {
    min-height: 150px;
  }

  .chart-placeholder,
  .table-placeholder {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100px;
    background: var(--surface-secondary);
    border-radius: 8px;
    font-size: 2rem;
    border: 2px dashed var(--border-color);
    transition: all 0.3s ease;
  }

  .chart-placeholder:hover,
  .table-placeholder:hover {
    border-color: var(--primary-color);
    background: var(--surface-color);
  }

  /* Responsive */
  @media (max-width: 1200px) {
    .dashboard-grid {
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 1rem;
    }
    
    .hero-content h1 {
      font-size: 2.5rem;
    }
    
    .hero-placeholder {
      width: 180px;
      height: 180px;
    }
  }

  @media (max-width: 1024px) {
    .sidebar-layout {
      grid-template-columns: 200px 1fr;
    }
    
    .dashboard-grid {
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    }
    
    .hero-content h1 {
      font-size: 2.2rem;
    }
  }

  @media (max-width: 900px) {
    .dashboard-grid {
      grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
      gap: 0.75rem;
    }
    
    .dashboard-card {
      padding: 1rem;
    }
    
    .stat-card .stat-number {
      font-size: 2rem;
    }
    
    .hero-section {
      padding: 3rem 2rem;
      grid-template-columns: 1fr;
      text-align: center;
      gap: 2rem;
    }
    
    .hero-content h1 {
      font-size: 2rem;
    }
    
    .hero-placeholder {
      width: 150px;
      height: 150px;
    }
  }

  @media (max-width: 768px) {
    .hero-section {
      grid-template-columns: 1fr;
      text-align: center;
      padding: 3rem 1.5rem;
      gap: 2rem;
    }
    
    .hero-content h1 {
      font-size: 2rem;
    }
    
    .hero-content p {
      font-size: 1.1rem;
    }
    
    .sidebar-layout {
      grid-template-columns: 1fr;
    }
    
    .sidebar {
      border-right: none;
      border-bottom: 1px solid var(--border-color);
    }
    
    .sidebar-nav {
      flex-direction: row;
      overflow-x: auto;
      padding: 0 1rem;
    }
    
    .nav-item {
      white-space: nowrap;
      padding: 0.75rem 1rem;
    }
    
    .dashboard-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .layouts-showcase {
      gap: 2rem;
    }
  }

  @media (max-width: 480px) {
    .hero-section {
      padding: 2rem 1rem;
    }
    
    .hero-content h1 {
      font-size: 1.8rem;
    }
    
    .hero-actions {
      flex-direction: column;
      width: 100%;
    }
    
    .hero-actions .btn {
      width: 100%;
      justify-content: center;
    }
    
    .hero-placeholder {
      width: 120px;
      height: 120px;
      font-size: 2.5rem;
    }
    
    .main-content {
      padding: 1.5rem;
    }

    .component-preview {
      padding: 1.5rem;
    }
    
    .dashboard-grid {
      grid-template-columns: 1fr;
      gap: 0.5rem;
    }
    
    .dashboard-card {
      padding: 0.75rem;
      min-width: 0;
    }
    
    .stat-card .stat-number {
      font-size: 1.8rem;
    }
  }
</style>
